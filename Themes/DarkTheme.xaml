<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Backgrounds -->
    <SolidColorBrush x:Key="WindowBg"       Color="#1E1E1E"/>
    <SolidColorBrush x:Key="SurfaceBg"      Color="#252526"/>
    <SolidColorBrush x:Key="SearchBg"       Color="#3C3C3C"/>
    <SolidColorBrush x:Key="SearchBorder"   Color="#555555"/>
    <SolidColorBrush x:Key="ListBg"         Color="#252526"/>
    <SolidColorBrush x:Key="ListAltBg"      Color="#2D2D30"/>
    <SolidColorBrush x:Key="ListSelectedBg" Color="#094771"/>
    <SolidColorBrush x:Key="SplitterBg"     Color="#3F3F46"/>
    <SolidColorBrush x:Key="QueryLabelBg"   Color="#1A2744"/>
    <SolidColorBrush x:Key="StatusBg"       Color="#007ACC"/>
    <SolidColorBrush x:Key="FuzzyBg"        Color="#3E2A00"/>
    <SolidColorBrush x:Key="DetailPaneBg"   Color="#1E1E1E"/>
    <SolidColorBrush x:Key="SuggestBg"      Color="#2D2D30"/>
    <SolidColorBrush x:Key="ButtonBg"       Color="#3C3C3C"/>
    <SolidColorBrush x:Key="ButtonHoverBg"  Color="#505050"/>

    <!-- Text -->
    <SolidColorBrush x:Key="TextPrimary"    Color="#D4D4D4"/>
    <SolidColorBrush x:Key="TextSecondary"  Color="#9E9E9E"/>
    <SolidColorBrush x:Key="TextHeadword"   Color="#90CAF9"/>
    <SolidColorBrush x:Key="TextAccent"     Color="#64B5F6"/>
    <SolidColorBrush x:Key="TextFuzzy"      Color="#FFB74D"/>
    <SolidColorBrush x:Key="StatusFg"       Color="#FFFFFF"/>

    <!-- Accent -->
    <SolidColorBrush x:Key="AccentBrush"    Color="#1565C0"/>
    <SolidColorBrush x:Key="DividerBrush"   Color="#3F3F46"/>

    <!-- ListView item style -->
    <Style x:Key="ListViewItemStyle" TargetType="ListViewItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Padding" Value="4,3"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource ListSelectedBg}"/>
                <Setter Property="Foreground" Value="#FFFFFF"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource QueryLabelBg}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Tool button style -->
    <Style x:Key="ToolBtn" TargetType="Button">
        <Setter Property="Background"      Value="{DynamicResource ButtonBg}"/>
        <Setter Property="Foreground"      Value="{DynamicResource TextPrimary}"/>
        <Setter Property="BorderBrush"     Value="{DynamicResource DividerBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding"         Value="8,4"/>
        <Setter Property="Cursor"          Value="Hand"/>
        <Setter Property="Margin"          Value="4,0,0,0"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource ButtonHoverBg}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
